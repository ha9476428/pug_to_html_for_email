//- Nội dung demo "Footer" — partial dùng chung cho mọi email (src/partials/footer.pug),
//- không phải mixin trong src/mixins/.
+section({ padding: ['lg', 'lg', 'sm'] })
  +heading(2) Footer
  +spacer('xs')
  +text({ color: theme.color.textMuted }) Footer mặc định dùng trong mọi email — src/partials/footer.pug

include ../partials/footer
