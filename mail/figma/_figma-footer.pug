//- Nội dung demo "Footer" — partial dùng chung cho mọi email (mail/partials/footer.pug),
//- không phải mixin trong mail/mixins/.
+section({ padding: ['lg', 'lg', 'sm'] })
  +heading(2) Footer
  +spacer('xs')
  +text({ color: theme.color.textMuted }) Footer mặc định dùng trong mọi email — mail/partials/footer.pug

include ../partials/footer
