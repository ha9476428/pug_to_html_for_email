//- Nội dung demo "Image" — dùng chung bởi image.pug (trang riêng) và index.pug (trang tổng hợp).
+section({ padding: ['lg', 'lg', 'sm'] })
    +heading(2) Image
    +spacer('xs')
    +text({ color: theme.color.textMuted }) image(src, alt, width, opts) — mail/mixins/_content.pug

+section({ padding: [0, 'lg', 'lg'] })
    +image('https://placehold.co/552x160/E5E7EB/6B7280?text=image(src,+alt,+width)', 'Ảnh mẫu', 552)
