//- Nội dung demo "Buttons" — dùng chung bởi buttons.pug (trang riêng) và index.pug (trang tổng hợp).
+section({ padding: ['lg', 'lg', 'sm'] })
    +heading(2) Buttons
    +spacer('xs')
    +text({ color: theme.color.textMuted }) button(label, href, opts) — mail/mixins/_content.pug

+section({ padding: [0, 'lg', 'lg'], align: 'left' })
    +button('Primary', '#', { align: 'left' })
    +spacer('sm')
    +button('Secondary', '#', { bg: theme.color.secondary, align: 'left' })
    +spacer('sm')
    +button('Danger', '#', { bg: theme.color.danger, align: 'left' })
    +spacer('sm')
    +button('Small', '#', { width: 140, height: 40, align: 'left' })
