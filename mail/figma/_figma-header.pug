//- Nội dung demo "Header" — partial dùng chung cho mọi email (mail/partials/header.pug),
//- không phải mixin trong mail/mixins/.
+section({ padding: ['lg', 'lg', 'sm'] })
  +heading(2) Header
  +spacer('xs')
  +text({ color: theme.color.textMuted }) Header mặc định dùng trong mọi email — mail/partials/header.pug

include ../partials/header
