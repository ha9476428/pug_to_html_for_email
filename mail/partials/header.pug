//- Header mặc định: logo thương hiệu
+section({ padding: ['lg', 'lg', 'md'], align: 'left' })
  +image(theme.brand.logo, theme.brand.name, theme.brand.logoWidth, { href: theme.brand.url, align: 'left', height: theme.brand.logoHeight })
