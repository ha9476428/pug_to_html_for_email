{
  "name": "pug_to_html_for_email",
  "version": "1.0.0",
  "description": "2 project tách riêng: email/ (Pug -> HTML inline CSS, tương thích email client) và landing/ (HTML/CSS viết tay, responsive).",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "email",
    "landing"
  ],
  "scripts": {
    "dev:email": "npm run dev -w email",
    "build:email": "npm run build -w email",
    "build:email:pretty": "npm run build:pretty -w email",
    "dev:landing": "npm run dev -w landing",
    "test:pixel": "npm run test:pixel -w landing"
  },
  "engines": {
    "node": ">=20"
  }
}
