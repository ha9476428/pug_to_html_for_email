{
  "name": "landing",
  "version": "1.0.0",
  "description": "Landing page HTML/CSS/JS viết tay (responsive, giữ nguyên @media) + test pixel-perfect.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "node scripts/dev.js",
    "pretest:pixel": "playwright install chromium",
    "test:pixel": "node scripts/pixel-test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "pixelmatch": "^7.2.0",
    "playwright": "^1.63.0",
    "pngjs": "^7.0.0"
  }
}
